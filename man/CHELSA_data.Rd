% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DWF_CHELSA_process.R, R/DWF_CHELSA_prepare.R,
%   R/DWF_CHELSA_project.R
\name{CHELSA_data}
\alias{CHELSA_data}
\alias{CHELSA_process}
\alias{CHELSA_prepare}
\alias{CHELSA_project}
\title{Process CHELSA Climate Data for the \code{IASDT}}
\usage{
CHELSA_process(
  env_file = ".env",
  n_cores = 8L,
  strategy = "multicore",
  download = FALSE,
  overwrite = FALSE,
  download_attempts = 10L,
  sleep = 5L,
  other_variables = "npp",
  download_n_cores = 4,
  compression_level = 5,
  overwrite_processed = FALSE
)

CHELSA_prepare(
  env_file = ".env",
  download = FALSE,
  n_cores = 8L,
  strategy = "multicore",
  overwrite = FALSE,
  download_attempts = 10L,
  sleep = 5L,
  other_variables = "npp"
)

CHELSA_project(metadata = NULL, env_file = ".env", compression_level = 5L)
}
\arguments{
\item{env_file}{Character. Path to the environment file containing paths to
data sources. Defaults to \code{.env}.}

\item{n_cores}{Integer. Number of CPU cores to use for parallel processing.
Default: 8.}

\item{strategy}{Character. The parallel processing strategy to use. Valid
options are "sequential", "multisession", "multicore", and "cluster".
Defaults to \code{"multicore"} (\code{"multisession"} on Windows). See
\code{\link[future:plan]{future::plan()}} and \code{\link[ecokit:set_parallel]{ecokit::set_parallel()}} for details.}

\item{download}{Logical. If \code{TRUE}, downloads CHELSA files. Default: \code{FALSE}.}

\item{overwrite}{Logical. If \code{TRUE}, re-downloads existing files. Default:
\code{FALSE}.}

\item{download_attempts}{Integer. Maximum download retries. Default: \code{10}.}

\item{sleep}{Integer. Seconds to wait between download attempts. Default:
\code{5}.}

\item{other_variables}{Character. Additional variables to process (e.g.,
\code{"npp"} for Net Primary Productivity alongside 19 bioclimatic variables
bio1-bio19). Use \code{""} for bioclimatic only. See \link{CHELSA_variables} for
details. Default: \code{"npp"}.}

\item{download_n_cores}{Integer. Number of CPU cores to use for parallel
downloading of CHELSA data. Only valid if download = \code{TRUE}. Defaults to 4.}

\item{compression_level}{Integer. NetCDF compression level (1 = least, 9 =
most). Default: \code{5}.}

\item{overwrite_processed}{Logical. If \code{TRUE}, overwrites processed files.
Default: \code{FALSE}.}

\item{metadata}{Tibble. Single-row metadata for input files, prepared by
\code{CHELSA_prepare()}}
}
\description{
Downloads, processes, and projects \href{https://chelsa-climate.org/}{CHELSA}
climate data at the European scale for the Invasive Alien Species Digital
Twin (\code{IASDT}). Supports multiple climate scenarios, outputting data in TIFF
and NetCDF formats. Orchestrated by \code{CHELSA_process()}, with helper functions
\code{CHELSA_prepare()} and \code{CHELSA_project()}.
}
\note{
\itemize{
\item \code{CHELSA_prepare()} and \code{CHELSA_project()} are internal helpers, not for
direct use.
\item Processes 19 bioclimatic variables (bio1–bio19) plus optional variables
(e.g., NPP) for 46 scenarios (1 current, 45 future).
\item Time-intensive; depends on file size and compute resources.
}
}
\section{Functions details}{

\itemize{
\item \strong{\code{CHELSA_process()}}: Main function; optionally downloads CHELSA data,
processes it to the European scale and reference grid, and saves TIFF and
NetCDF outputs for 46 climate scenarios.
\item \strong{\code{CHELSA_prepare()}}: Extracts metadata from local URL text files and
manages optional downloads.
\item \strong{\code{CHELSA_project()}}: Projects data to the \code{IASDT} reference grid with
optional transformations.
}
}

\author{
Ahmed El-Gabbas
}
